[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev -- --port 3000"
  targetPort = 3000
  functionsPort = 9999
  autoLaunch = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
